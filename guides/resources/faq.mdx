---
title: "FAQ"
description: "Frequently asked questions about Upmore."
icon: "circle-question"
---

<AccordionGroup>
  <Accordion title="What is Upmore?">
    Upmore is a unified AI API platform that gives developers access to models from multiple providers — including chat, image generation, and video generation — through a single API key and consistent interface.
  </Accordion>

  <Accordion title="How do I get an API key?">
    Sign up at [upmore.net](https://api.upmore.net), then go to the [Console](https://api.upmore.net/console/token) to create your API key.
  </Accordion>

  <Accordion title="Is Upmore compatible with the OpenAI SDK?">
    Yes. For chat models, Upmore follows the OpenAI API specification. You only need to change the `base_url` to `https://api.upmore.net/v1` — no other code changes required.
  </Accordion>

  <Accordion title="How does billing work?">
    Upmore uses a prepaid credit system. Credits are deducted in real time as you make API calls. Chat models are billed per token, image and video models are billed per generation. Visit the [Top Up page](https://api.upmore.net/console/topup) to add credits.
  </Accordion>

  <Accordion title="What happens if I run out of credits?">
    API calls will return a `402 Payment Required` error. Top up your account at the [Console](https://api.upmore.net/console/topup) to resume.
  </Accordion>

  <Accordion title="Which models are supported?">
    Upmore supports models from multiple providers including Anthropic, ByteDance, and more. Browse the full list in the [Model APIs](/guides/model-api/bytedance/seedance-2-0) section.
  </Accordion>

  <Accordion title="How do I generate a video with Seedance 2.0?">
    Video generation is asynchronous. Submit a request to `POST /v1/video/generations` and poll `GET /v1/video/generations/{task_id}` until the status is `succeeded`. See the [Seedance 2.0 guide](/guides/model-api/bytedance/seedance-2-0) for a full example.
  </Accordion>
</AccordionGroup>
